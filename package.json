{
  "name": "secondhand-trade",
  "version": "1.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "backend/**",
      "frontend/**",
      "packages/**"
    ]
  },
  "scripts": {
    "db:start": "docker-compose up -d",
    "db:stop": "docker-compose down",
    "db:restart": "yarn db:stop && yarn db:start",
    "server": "yarn workspace @secondhand-trade/server"
  },
  "repository": "https://github.com/bluemiv/secondhand-trade.git",
  "author": "taehongkim <public.bluemiv@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.1.0"
  }
}
